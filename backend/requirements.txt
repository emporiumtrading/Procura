# Procura Backend Dependencies
# Versions verified from working local environment (pip freeze)

# Web Framework
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.22

# Database / Supabase
supabase==2.27.2
postgrest==2.28.3
realtime==2.27.2
storage3==2.27.2
supabase-auth==2.27.2
supabase-functions==2.27.2

# HTTP Client
httpx==0.28.1
email-validator==2.3.0

# Security
cryptography==46.0.4
python-jose[cryptography]==3.5.0

# LLM Provider — Google Gemini (production default, PROCURA_LLM_PROVIDER=google)
# anthropic and openai are lazy-imported and NOT required for production
google-genai==1.63.0

# Configuration
python-dotenv==1.2.1
pydantic==2.12.5
pydantic-settings==2.13.0

# Utilities
python-dateutil==2.9.0.post0
tenacity==9.1.2
structlog==25.5.0

# Rate Limiting
slowapi==0.1.9

# Error Reporting (no-op when SENTRY_DSN is not set)
sentry-sdk[fastapi]>=2.0.0

# Task Queue (only active when REDIS_URL is set)
celery==5.6.2
redis==7.2.0

# Procura Backend Dependencies

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# Database
supabase==2.10.0
postgrest==0.18.0

# HTTP Client
httpx==0.28.1
aiohttp==3.11.11
email-validator==2.2.0

# Security
cryptography==44.0.0
python-jose[cryptography]==3.3.0

# LLM Providers — only google-genai is required in production (render.yaml: PROCURA_LLM_PROVIDER=google).
# anthropic and openai are imported lazily and only needed if you switch providers.
google-genai==1.9.0

# Configuration
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0

# Utilities
python-dateutil==2.9.0.post0
tenacity==9.0.0
structlog==24.4.0

# Rate Limiting
slowapi==0.1.9

# Error Reporting (optional — no-op when SENTRY_DSN not set)
sentry-sdk[fastapi]==2.19.2

# Task Queue (optional — only needed if REDIS_URL is set for Celery)
celery==5.4.0
redis==5.2.1
